/* Miracolo Lab V1 — high-density source menu renderer. */
(() => {
  'use strict';
  const MAX_PER_CATEGORY = 500;
  const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
  const groups=[['news','Notizie','📰'],['finance','Finance','💹'],['crypto','Cripto','₿'],['macro','Macroeconomia','🌍'],['rates','Obbligazioni / Tassi','💶'],['central','Banche Centrali','🏦'],['commodities','Commodities','🛢️'],['fx','Forex','💱'],['volatility','Volatilità','📊'],['geopolitics','Geopolitica','🌐'],['social','Social / Sentiment','💬'],['company','Società / Earnings','🏢']];
  function group(x){const raw=`${x.cat||x.type||''} ${x.source||''} ${x.title||''} ${x.description||''}`.toLowerCase(),t=String(x.cat||x.type||'').toLowerCase();if(t==='crypto'||/crypto|bitcoin|ethereum|solana|xrp|coindesk/.test(raw))return'crypto';if(t==='macro'||/inflation|cpi|pce|gdp|payroll|pmi|fred|bls|bea|eurostat|liquidity/.test(raw))return'macro';if(t==='rates'||/treasury|bond|yield|bund|btp|gilt|credit spread|high yield|cds/.test(raw))return'rates';if(t==='central'||/fed|ecb|boe|boj|snb|rba|central bank/.test(raw))return'central';if(t==='commodities'||/gold|silver|oil|brent|wti|copper|opec|gas/.test(raw))return'commodities';if(t==='fx'||/forex|eurusd|eur\/usd|dxy|usd[jy]|gbp|sterling|dollar|yen/.test(raw))return'fx';if(t==='volatility'||/vix|volatility|options|gamma|put.?call|open interest/.test(raw))return'volatility';if(t==='geopolitics'||/geopolitic|sanction|tariff|war|conflict|ukraine|russia|iran|israel/.test(raw))return'geopolitics';if(t==='social'||/reddit|social|sentiment|wallstreetbets/.test(raw))return'social';if(t==='company'||/earnings|company|corporate|sec filing|revenue|guidance|buyback|jpmorgan|goldman|banking/.test(raw))return'company';if(/finance|market|stock|equity|etf|nasdaq|s&p|dow|dax|nikkei|ftse|msci/.test(raw))return'finance';return'news';}
  function article(x){const href=x.url||x.link||'',title=esc(x.title||x.headline||'Articolo senza titolo'),source=esc(x.source||x.feed||x.publisher||'Fonte'),score=Number(x.score||0),tone=score>0?'POSITIVO':score<0?'NEGATIVO':'NEUTRALE',body=`<span class="v1-article-title">${title}</span><span class="v1-article-meta">${source} · ${tone}</span>`;return href?`<a class="v1-article" href="${esc(href)}" target="_blank" rel="noopener noreferrer">${body}</a>`:`<div class="v1-article">${body}</div>`;}
  function render(state){const root=document.querySelector('.v1-source-groups'),items=state?.scan?.items||state?.scan?.signals||state?.scan?.results||[];if(!root||!items.length)return;const buckets=Object.fromEntries(groups.map(([k])=>[k,[]]));items.forEach(x=>buckets[group(x)].push(x));root.innerHTML=groups.map(([k,label,icon])=>{const a=buckets[k],shown=a.slice(0,MAX_PER_CATEGORY);return `<details class="v1-category" ${k==='news'||k==='finance'?'open':''}><summary>${icon} ${label}<span>${a.length}</span></summary><div>${shown.map(article).join('')}${a.length>MAX_PER_CATEGORY?`<div class="v1-density-note">Visualizzate ${MAX_PER_CATEGORY} di ${a.length} notizie.</div>`:''}${!a.length?'<div class="v1-empty">In attesa di nuove fonti.</div>':''}</div></details>`;}).join('');}
  window.ML?.on('state',render);
  document.addEventListener('DOMContentLoaded',()=>{if(window.ML?.state)render(window.ML.state);});
})();