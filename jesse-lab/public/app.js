const button=document.querySelector('#backtest');const result=document.querySelector('#result');button.addEventListener('click',async()=>{button.disabled=true;button.textContent='RUNNING…';result.classList.remove('hidden');result.textContent='Research session placeholder: the Jesse backend will execute this preset in the next phase.';try{const r=await fetch('/api/health');const d=await r.json();result.textContent=`Backend gateway: ${d.ok?'ONLINE':'OFFLINE'} · Jesse execution: ${d.jesse}`;}catch(e){result.textContent='Gateway check failed.'}button.disabled=false;button.textContent='RUN BACKTEST';});
